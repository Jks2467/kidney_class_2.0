Logging string
logging.basicConfig(level=logging.INFO, format='%(asctime)s - %(levelname)s - %(message)s')


# to set the path to src so that imports can be done considering folders as python packages
set PYTHONPATH=%cd%\src
